class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        cursum=0
        max_sum=float("-inf")
        for i in range(0,len(nums)):
            cursum+=nums[i]
            max_sum=max(max_sum,cursum)
            if cursum<0:
                cursum=0
        return max_sum

        
